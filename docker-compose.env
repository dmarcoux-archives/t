# Same as service 'database' from docker-compose.yml
POSTGRES_HOST=database
# Override default values in postgres Dockerfile for the service 'database' from docker-compose.yml
POSTGRES_USER=t
POSTGRES_PASSWORD=t-password
POSTGRES_DB=t-db
